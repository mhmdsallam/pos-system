# 🚀 خطة تحويل النظام (Roadmap to Retail & Pharmacy)

هذا الدليل يوضح الخطوات التقنية اللازمة لتحويل نظام المطاعم الحالي (`Restaurant POS`) إلى أنظمة أخرى، مع الحفاظ على الكود الأساسي (`Core System`).

---

## 🏗️ 1. نظام السوبر ماركت (Supermarket / Grocery Mode)

**الهدف:** تحويل شاشة البيع للتركيز على السرعة (Scanning) وإدارة التواريخ والكميات الكبيرة.

### ✅ التعديلات المطلوبة (Checklist):

- [ ] **قاعدة البيانات (Database):**
  - إضافة عمود `barcode` (فهرس فريد - Unique Index).
  - إضافة عمود `expiry_date` (تاريخ الصلاحية).
  - إضافة عمود `wholesale_price` (سعر الجملة/الكرتونة).
  - إضافة عمود `brand` (الماركة).

- [ ] **واجهة البيع (POS UI):**
  - **Focus Mode:** جعل مؤشر الكتابة دائماً في خانة البحث (للقراءة التلقائية من الباركود).
  - **List View:** إلغاء عرض الصور (Grid) واستبداله بجدول بيانات سريع (الاسم، السعر، الكمية المتاحة).
  - **Hide Restaurant Features:** إخفاء (رقم الطاولة، المطبخ، أنواع الطلبات).

- [ ] **المخزون (Inventory):**
  - **Low Stock Alerts:** تنبيهات نواقص متقدمة.
  - **Expired Items Report:** تقرير بالمنتجات التي قاربت على الانتهاء.

- [ ] **الاستيراد والتصدير:**
  - **Excel Import:** ميزة إلزامية لاستيراد آلاف الأصناف دفعة واحدة.

---

## 💊 2. نظام الصيدليات (Pharmacy Mode)

**الهدف:** دقة متناهية في التواريخ والوحدات (علبة vs شريط).

### ✅ التعديلات المطلوبة (Checklist):

- [ ] **قاعدة البيانات (Database):**
  - إضافة جدول `batches` (رقم التشغيلة) لكل دواء.
  - إضافة `unit_conversion` (عامل تحويل: العلبة فيها كام شريط؟).
  - إضافة `generic_name` (الاسم العلمي) للبحث عن البدائل.

- [ ] **واجهة البيع (POS UI):**
  - **Search Alternatives:** عند البحث عن دواء ناقص، يقترح "البديل" بنفس المادة الفعالة.
  - **Unit Selection:** إمكانية بيع (علبة كاملة) أو (شريط) أو (قرص) بسعر دقيق.

- [ ] **المخزون (Inventory):**
  - **expiry_check:** منع بيع أي دواء منتهي الصلاحية أوتوماتيكياً.

---

## 👗 3. نظام محلات الملابس (Fashion / Retail Mode)

**الهدف:** إدارة المخزون بناءً على الألوان والمقاسات (Matrix).

### ✅ التعديلات المطلوبة (Checklist):

- [ ] **قاعدة البيانات (Database):**
  - تحويل `product_variations` لتشمل: `size`, `color`, `material`.
  - ربط كل "لون + مقاس" ببارود مختلف (SKU).

- [ ] **الطباعة (Printing):**
  - **Barcode Generator:** أداة لتوليد وطباعة "استيكر" الباركود للملابس التي لا تحمل باركود مصنعي.

- [ ] **سياسة الاسترجاع (Returns):**
  - نظام استرجاع وتبديل صارم (التحقق من الفاتورة الأصلية وحالة القطعة).

---

## 🛠️ كيفية البدء (How to Start)

عندما تقرر البدء في أي نشاط من هؤلاء، نفذ الخطوات التالية:

1.  **Duplicate Project:** انسخ مجلد المشروع الحالي بالكامل.
2.  **Rename Folder:** سمّ المجلد الجديد باسم النشاط (مثلاً `Pharmacy-POS`).
3.  **Open in VS Code:** افتح المجلد الجديد.
4.  **Tell Me:** "ابدأ في تحويل النسخة لنظام صيدلية".

---

> **ملاحظة:** النسخة الحالية (`Restaurant POS`) جاهزة وتعمل كملف `exe` في مجلد `dist-packaged`. هذا الملف هو خطتك للمستقبل. 🚀
